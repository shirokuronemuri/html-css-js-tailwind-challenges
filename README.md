# html-css-tailwind-challenges

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev
```

To build:
```
bun run build
```

You can check the build with:
```
bun run serve
```
